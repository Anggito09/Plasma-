import { integer, sqliteTable, text, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const settings = sqliteTable('settings', { id: integer('id').primaryKey(), name: text('name').notNull(), timezone: text('timezone').notNull(), footer: text('footer').notNull(), targetReadyMin: integer('target_ready_min').notNull().default(10) });
export const users = sqliteTable('users', { id: text('id').primaryKey(), username: text('username').notNull().unique(), name: text('name').notNull(), password: text('password').notNull(), role: text('role').notNull(), active: integer('active').notNull().default(1) });
export const sessions = sqliteTable('sessions', { token: text('token').primaryKey(), userId: text('user_id').notNull().references(()=>users.id), expires: integer('expires').notNull() });
export const attempts = sqliteTable('attempts', { key: text('key').primaryKey(), count: integer('count').notNull(), until: integer('until').notNull() });
export const products = sqliteTable('products', { id: text('id').primaryKey(), name: text('name').notNull(), category: text('category').notNull(), price: integer('price').notNull(), active: integer('active').notNull().default(1) });
export const payments = sqliteTable('payments', { id: text('id').primaryKey(), name: text('name').notNull().unique(), active: integer('active').notNull().default(1) });
export const orders = sqliteTable('orders', { id: text('id').primaryKey(), day: text('day').notNull(), number: integer('number').notNull(), customer: text('customer').notNull(), mode: text('mode').notNull(), items: text('items').notNull(), note: text('note').notNull(), total: integer('total').notNull(), payment: text('payment').notNull(), status: text('status').notNull(), createdAt: integer('created_at').notNull(), updatedAt: integer('updated_at').notNull(), createdBy: text('created_by').notNull(), fingerprint: text('fingerprint').notNull(), version: integer('version').notNull().default(1), cancelReason: text('cancel_reason'), preparedAt: integer('prepared_at'), readyAt: integer('ready_at'), completedAt: integer('completed_at') },t=>[uniqueIndex('orders_day_number').on(t.day,t.number),index('orders_status_created').on(t.status,t.createdAt),index('orders_day_created').on(t.day,t.createdAt)]);
export const events = sqliteTable('events', { id: integer('id').primaryKey({autoIncrement:true}), requestId: text('request_id').notNull().unique(), orderId: text('order_id').notNull().references(()=>orders.id), day: text('day').notNull(), number: integer('number').notNull(), customer: text('customer').notNull(), createdAt: integer('created_at').notNull() });
export const expenses = sqliteTable('expenses', { id: text('id').primaryKey(), day: text('day').notNull(), category: text('category').notNull(), note: text('note').notNull(), amount: integer('amount').notNull(), createdAt: integer('created_at').notNull(), createdBy: text('created_by').notNull() }, t => [index('expenses_day_created').on(t.day, t.createdAt)]);
export const audit = sqliteTable('audit', { id: integer('id').primaryKey({autoIncrement:true}), actor: text('actor').notNull(), action: text('action').notNull(), target: text('target').notNull(), createdAt: integer('created_at').notNull() });
